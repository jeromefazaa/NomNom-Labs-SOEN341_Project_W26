const express = require('express');
const db = require('../../database');

const router = express.Router();


// create recipe

// router.post("/", (req,res)=>{
//     const {title, ingredients, steps,cost, userId} = 
// })



module.exports = router;
